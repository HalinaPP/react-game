import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';

const root = document.getElementById('root');

//root?.addEventListener('keydown',handleKeyPress);

ReactDOM.render(<App />, root);
